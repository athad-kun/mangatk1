# 📚 توثيق مشروع MangaTK - الملف الرئيسي

**آخر تحديث**: 6 ديسمبر 2024

---

## 🎯 نظرة عامة على المشروع

**MangaTK** هو منصة لقراءة المانجا مبنية على:
- **Backend**: Django + Django REST Framework
- **Frontend**: Next.js + TypeScript + Tailwind CSS
- **Database**: SQLite (يمكن تغييرها لـ PostgreSQL)
- **Image Storage**: ImgBB API

---

## 📋 سجل التطوير الزمني

### المرحلة 1: إعداد Backend (ديسمبر 2024)

#### ✅ ما تم إنجازه:

1. **هيكلة المشروع**
   - فصل `backend/` و `frontend/` عن بعضهما
   - نقل بيئة Django الموجودة إلى `backend/`
   - إنشاء Django project باسم `config`
   - إنشاء تطبيق `manga`

2. **قاعدة البيانات الأولية (5 جداول)**
   - `Genre`: أنواع المانجا (14 نوع)
   - `Category`: التصنيفات (5 تصنيفات)
   - `Manga`: بيانات المانجا (18 مانجا)
   - `Chapter`: الفصول (0 حالياً)
   - `ChapterImage`: صور الفصول (0 حالياً)

3. **Django REST API**
   - Serializers لجميع النماذج
   - ViewSets مع فلاتر (search, category, genre, status)
   - Pagination
   - Custom actions (featured, chapters)
   - CORS configuration

4. **Admin Panel**
   - واجهة إدارة كاملة لجميع النماذج
   - معاينة الصور
   - Inline editing
   - Filters وبحث متقدم

5. **خدمة ImgBB**
   - `ImgBBService` class
   - تسمية موحدة: `{manga_slug}_cover`, `{manga_slug}_ch{num}_page{num}`
   - API Key: `9acd5ae77545c9653ac31a3d680fb638`

6. **استيراد البيانات**
   - أمر `import_mock_data` لاستيراد من TypeScript mock files
   - أمر `update_cover_images` لتحديث صور الغلاف
   - **النتيجة**: 18 مانجا، 14 نوع، 5 تصنيفات

**الملفات المُنشأة**:
```
backend/
├── config/
│   ├── settings.py (تم التعديل)
│   └── urls.py (تم التعديل)
├── manga/
│   ├── models.py (✅ جديد)
│   ├── serializers.py (✅ جديد)
│   ├── views.py (✅ جديد)
│   ├── urls.py (✅ جديد)
│   ├── admin.py (✅ جديد)
│   ├── services/
│   │   └── imgbb.py (✅ جديد)
│   └── management/commands/
│       ├── import_mock_data.py (✅ جديد)
│       └── update_cover_images.py (✅ جديد)
└── db.sqlite3 (18 manga)
```

---

### المرحلة 2: Frontend Integration (ديسمبر 2024)

#### ✅ ما تم إنجازه:

1. **API Service Layer**
   - إنشاء `frontend/src/services/api.ts` (300سطر)
   - 15 دالة API
   - معالجة أخطاء
   - تحويل بيانات تلقائي

2. **تحديث المكونات**
   - `page.tsx` - الصفحة الرئيسية (✅ يستخدم API)
   - `manga/[id]/page.tsx` - تفاصيل المانجا (✅ يستخدم API)
   - `category/[slug]/page.tsx` - صفحات التصنيفات (✅ يستخدم API)

3. **إزالة Mock Data**
   - ❌ لا يوجد أي استخدام لـ mock data
   - ✅ جميع البيانات من Django API

4. **Loading & Error States**
   - Loading spinners
   - Error handling
   - Empty states

**الملفات المُنشأة/المُعدّلة**:
```
frontend/
├── src/
│   ├── services/
│   │   └── api.ts (✅ جديد - 268 سطر)
│   └── app/
│       ├── page.tsx (🔄 مُعدّل - API)
│       ├── manga/[id]/page.tsx (🔄 مُعدّل - API)
│       └── category/[slug]/page.tsx (🔄 مُعدّل - API)
└── .env.local (يُنشأ يدوياً)
```

**Environment Variables**:
```env
NEXT_PUBLIC_API_URL=http://localhost:8000/api
```

---

### المرحلة 3: تحديث هيكل قاعدة البيانات (جاري التخطيط) 🚧

#### 📋 التحديثات المخططة:

**الجداول الجديدة (8)**:
1. ⭐ `User` - نظام المستخدمين
2. ⭐ `UserBookmark` - المفضلات
3. ⭐ `ReadingHistory` - سجل القراءة
4. ⭐ `Rating` - التقييمات
5. ⭐ `Comment` - التعليقات
6. ⭐ `CommentLike` - إعجابات التعليقات
7. ⭐ `Achievement` - الإنجازات
8. ⭐ `UserAchievement` - إنجازات المستخدمين

**التحديثات على الجداول الموجودة**:
- `Manga`: إضافة `sub_titles` للأسماء البديلة
- `Chapter`: حساب تلقائي لـ `number` وتاريخ النشر

**المجموع الكلي**: 13 جدول

---

## 🗂️ هيكل قاعدة البيانات الحالي

### الجداول (5 موجودة):

| الجدول | الصفوف | الحالة | الوصف |
|--------|--------|--------|-------|
| **Genre** | 14 | ✅ | أنواع المانجا |
| **Category** | 5 | ✅ | التصنيفات الرئيسية |
| **Manga** | 18 | ✅ | بيانات المانجا مع صور |
| **Chapter** | 0 | ⚠️ | الفصول (فارغ) |
| **ChapterImage** | 0 | ⚠️ | صور الفصول (فارغ) |

### العلاقات الحالية:

```
Genre ←→ Manga (Many-to-Many)
Category → Manga (One-to-Many)
Manga → Chapter (One-to-Many)
Chapter → ChapterImage (One-to-Many)
```

---

## 🔧 التكوينات الرئيسية

### Backend (`settings.py`):

```python
INSTALLED_APPS = [
    'manga',
    'rest_framework',
    'corsheaders',
    # ...
]

CORS_ALLOWED_ORIGINS = [
    "http://localhost:3000",
    "http://127.0.0.1:3000",
]

REST_FRAMEWORK = {
    'DEFAULT_PAGINATION_CLASS': 'rest_framework.pagination.PageNumberPagination',
    'PAGE_SIZE': 20,
}

IMGBB_API_KEY = '9acd5ae77545c9653ac31a3d680fb638'
IMGBB_API_URL = 'https://api.imgbb.com/1/upload'
```

### Frontend (`api.ts`):

```typescript
const API_BASE_URL = process.env.NEXT_PUBLIC_API_URL || 'http://localhost:8000/api';
```

---

## 📡 API Endpoints

### Manga:
- `GET /api/manga/` - قائمة المانجا
- `GET /api/manga/{id}/` - تفاصيل مانجا
- `GET /api/manga/{id}/chapters/` - فصول مانجا
- `GET /api/manga/featured/` - المانجا المميزة
- `POST /api/manga/` - إضافة مانجا

**Filters**:
- `?search=query` - البحث
- `?category=slug` - حسب التصنيف
- `?genre=name` - حسب النوع
- `?status=ongoing|completed` - حسب الحالة
- `?ordering=field` - الترتيب

### Chapters:
- `GET /api/chapters/` - قائمة الفصول
- `GET /api/chapters/{id}/` - تفاصيل فصل

### Categories & Genres:
- `GET /api/categories/` - قائمة التصنيفات
- `GET /api/genres/` - قائمة الأنواع

---

## 🎨 Frontend Structure

### الصفحات الرئيسية:

| الصفحة | المسار | الحالة | الوصف |
|--------|--------|--------|-------|
| الرئيسية | `/` | ✅ | عرض جميع المانجا |
| تفاصيل المانجا | `/manga/[id]` | ✅ | معلومات وفصول |
| قراءة الفصل | `/read/[chapterId]` | ⚠️ | قارئ الصور |
| التصنيف | `/category/[slug]` | ✅ | مانجا حسب التصنيف |
| التسجيل | `/register` | ⚠️ | إنشاء حساب |
| تسجيل الدخول | `/login` | ⚠️ | الدخول |
| الملف الشخصي | `/profile` | ⚠️ | بيانات المستخدم |

### المكونات الرئيسية:

```
components/
├── Header.tsx - رأس الصفحة
├── Footer.tsx - تذييل الصفحة
├── ComicGrid.tsx - شبكة المانجا
├── FilterSection.tsx - الفلاتر
├── CategoryNav.tsx - شريط التصنيفات
├── Carousel.tsx - السلايدر
└── SectionTitle.tsx - عناوين الأقسام
```

---

## 🚀 كيفية التشغيل

### Backend:
```bash
cd d:\gndhn\mangatk\backend
.\Scripts\python.exe manage.py runserver
# → http://localhost:8000
```

### Frontend:
```bash
cd d:\gndhn\mangatk\frontend
npm run dev
# → http://localhost:3000
```

### Admin Panel:
```
URL: http://localhost:8000/admin/
```
لإنشاء superuser:
```bash
cd backend
.\Scripts\python.exe manage.py createsuperuser
```

---

## 📄 الملفات التوثيقية

| الملف | الموقع | الوصف |
|-------|--------|-------|
| `INTEGRATION_REPORT.md` | `/` | تقرير ربط Frontend-Backend |
| `DATABASE_SCHEMA.md` | `/` | هيكل قاعدة البيانات |
| `backend/README.md` | `/backend/` | دليل Backend |
| `implementation_plan.md` | Artifacts | خطة المرحلة 3 |

---

## 🔄 المراحل القادمة

### Phase 3: User System
- [ ] إنشاء نموذج User
- [ ] نظام التسجيل/تسجيل الدخول
- [ ] JWT Authentication
- [ ] صفحة الملف الشخصي

### Phase 4: Interactive Features
- [ ] نظام التقييمات
- [ ] نظام التعليقات
- [ ] المفضلات
- [ ] سجل القراءة

### Phase 5: Achievements
- [ ] نظام الإنجازات
- [ ] تتبع التقدم
- [ ] صفحة الإنجازات

### Phase 6: Chapter Upload System
- [ ] رفع الفصول (ZIP/CBZ)
- [ ] فك الضغط واستخراج الصور
- [ ] رفع تلقائي لـ ImgBB
- [ ] حفظ الروابط في قاعدة البيانات

### Phase 7: Advanced Features
- [ ] نظام الإشعارات
- [ ] البحث المتقدم
- [ ] Filters متقدمة
- [ ] Pagination محسّن

---

## 🔑 القرارات التقنية المهمة

### 1. تخزين الصور:
**القرار**: استخدام ImgBB + حفظ URLs فقط
**السبب**: 
- تقليل حجم قاعدة البيانات
- سهولة النسخ الاحتياطي
- CDN مجاني من ImgBB

### 2. UUIDs vs Auto-increment IDs:
**القرار**: استخدام UUID
**السبب**:
- أمان أعلى (عدم تخمين IDs)
- مناسب للتوسع المستقبلي
- عدم تضارب عند الدمج

### 3. Slugs:
**القرار**: استخدام slugs لجميع النماذج
**السبب**:
- SEO-friendly URLs
- أسهل للقراءة
- مناسب للـ routing

### 4. Computed Properties vs Database Fields:
**القرار**: حساب `chapter_count`, `avg_rating`, `last_updated` ديناميكياً
**السبب**:
- بيانات دائماً محدثة
- تجنب redundancy
- تقليل التعقيد

### 5. Mock Data:
**القرار**: الاحتفاظ لكن عدم الاستخدام
**السبب**:
- مرجع تاريخي
- سهولة الرجوع للبيانات الأولية
- اختبارات مستقبلية

---

## 📊 إحصائيات المشروع

### Backend:
- **Models**: 5 (سيصبح 13)
- **API Endpoints**: ~15
- **Management Commands**: 2
- **Services**: 1 (ImgBB)

### Frontend:
- **Pages**: 7
- **Components**: ~15
- **Services**: 1 (API)
- **API Functions**: 15

### Database:
- **Tables**: 5 (سيصبح 13)
- **Rows**: 37 (18 manga + 14 genres + 5 categories)

---

## 🐛 المشاكل المعروفة

1. **Memory Error في Next.js**
   - **الخطأ**: `mem_alloc(): Fatal_error: out of memory`
   - **الحل**: تشغيل من `frontend/` مباشرة أو زيادة memory limit

2. **الفصول فارغة**
   - **الحالة**: لا توجد فصول في قاعدة البيانات بعد
   - **الخطة**: انتظار نظام رفع ZIP/CBZ

3. **`.env.local` مفقود**
   - **الحل**: إنشاء يدوياً مع `NEXT_PUBLIC_API_URL=http://localhost:8000/api`

---

## 📝 ملاحظات التطوير

### Best Practices:
- ✅ استخدام TypeScript في Frontend
- ✅ REST API standards
- ✅ Proper error handling
- ✅ Loading states
- ✅ Responsive design
- ✅ Dark mode support

### Code Quality:
- ✅ توثيق شامل
- ✅ تعليقات واضحة
- ✅ تسميات وصفية
- ✅ DRY principle

---

## 🔗 الروابط المهمة

- **Frontend**: http://localhost:3000
- **Backend API**: http://localhost:8000/api/
- **Admin Panel**: http://localhost:8000/admin/
- **ImgBB**: https://api.imgbb.com/

---

## 👥 نظام الصلاحيات (مستقبلي)

### Roles:
1. **SuperAdmin** - صلاحيات كاملة
2. **Admin** - إدارة المحتوى
3. **Moderator** - إدارة التعليقات
4. **Premium User** - مميزات إضافية
5. **Regular User** - مستخدم عادي

---

## 🎯 الأهداف النهائية

- [x] Backend كامل مع API
- [x] Frontend متصل بـ API
- [ ] نظام مستخدمين
- [ ] نظام رفع الفصول
- [ ] تفاعل اجتماعي (تعليقات، تقييمات)
- [ ] نظام إنجازات
- [ ] Mobile responsive
- [ ] Performance optimization

---

**آخر تحديث في هذا الملف**: 6 ديسمبر 2024، الساعة 2:50 م

---

## 📞 للمساعدة

راجع الملفات التوثيقية الأخرى:
- `INTEGRATION_REPORT.md` - للتفاصيل التقنية
- `DATABASE_SCHEMA.md` - لهيكل قاعدة البيانات
- `implementation_plan.md` - للخطة القادمة
